<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{titulo}}</title>
    <style>
        :root {
            --lilac: #a79bd4;
            --ink: #111;
            --line: #cfc6ee;
            /* borde lila claro */
            --thead: #efecfb;
            /* fondo encabezado tabla */
        }

        /* === LETTER + márgenes ajustados para el membrete === */
        @page {
            size: Letter;
            margin: 16mm 26mm 50mm 16mm;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 12px;
            color: var(--ink);
            margin: 0;
            padding: 0;
            position: relative;
        }

        /* ===== Membrete como fondo fijo ===== */
        .bg-page {
            position: fixed;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
            overflow: hidden;
        }

        .bg-page img {
            height: 100%;
            position: absolute;
            right: 0;
            top: 0;
        }

        /* Contenedor principal para el contenido */
        .content {
            position: relative;
            z-index: 1;
        }

        /* Encabezado */
        .top {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 10px;
        }

        .fecha {
            color: #333;
        }

        /* Título centrado */
        .title {
            font-weight: 800;
            text-align: center;
            margin: 10px 0 8px;
            font-size: 16px;
        }

        /* Destinatario */
        .dest {
            margin-top: 8px;
            font-weight: 700;
            line-height: 1.35;
        }

        .presente {
            font-weight: 800;
            margin-top: 2px;
        }

        /* Resumen opcional */
        .resume {
            margin-top: 8px;
            color: #222;
            line-height: 1.5;
        }

        .label {
            font-weight: 700;
        }

        /* Descripción */
        .desc {
            margin-top: 10px;
            color: #222;
        }

        /* ===== Firma (arriba de la tabla, alineada a la derecha) ===== */
        .sig {
            margin-top: 20px;
            display: flex;
            justify-content: flex-end;
        }

        .sig-inner {
            text-align: right;
        }

        .sig-inner .atte {
            font-weight: 700;
            margin-bottom: 2px;
        }

        .sig-inner img {
            height: 110px;
            display: block;
            margin: 0 0 4px auto;
        }

        .sig-inner .name {
            font-weight: 700;
        }

        /* Tabla */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
        }

        thead th {
            background: var(--thead);
            color: #2a2740;
            border: 1px solid var(--line);
            padding: 7px 9px;
            font-size: 12px;
            text-align: left;
            font-weight: 700;
        }

        tbody td {
            border: 1px solid var(--line);
            padding: 6px 8px;
            font-size: 12px;
            background: #fff;
            vertical-align: top;
        }

        .center {
            text-align: center;
        }

        .right {
            text-align: right;
        }

        /* Totales (derecha) */
        .grand {
            display: flex;
            justify-content: flex-end;
            margin-top: 15px;
        }

        .grand table {
            width: 68mm;
            border-collapse: collapse;
        }

        .grand td {
            padding: 6px 8px;
            border-left: 1px solid var(--line);
            border-right: 1px solid var(--line);
        }

        .grand tr:first-child td {
            border-top: 1px solid var(--line);
        }

        .grand tr:last-child td {
            border-bottom: 1px solid var(--line);
        }

        .grand .label {
            font-weight: 700;
        }

        .grand .value {
            text-align: right;
        }

        /* Términos */
        .terms-title {
            font-weight: 800;
            margin: 16px 0 6px;
        }

        .terms {
            margin: 0;
            padding-left: 18px;
            color: #333;
        }
    </style>
</head>

<body>
    <!-- Fondo / membrete -->
    <div class="bg-page">
        <img src="assets/emp6.png" alt="membrete">
    </div>

    <div class="content">
        <!-- Fecha (arriba derecha) -->
        <div class="top">
            <div class="fecha">Zempoala, Hidalgo, a {{fecha}}</div>
        </div>

        <!-- Título -->
        <div class="title">Solicitud de Cotización</div>

        <!-- Destinatario -->
        <div class="dest">{{destinatario}}</div>
        <div class="presente">Presente.</div>

        <!-- Resumen (si lo mandas) -->
        <div class="resume">
            {{#if requisicion}}
            <div><span class="label">Requisición:</span> {{requisicion}}</div>
            {{/if}}
            <div><span class="label">Subtotal:</span> {{money totales.subtotal}}</div>
            <div><span class="label">Total:</span> {{money totales.total}}</div>
            {{#if totalEnLetra}}
            <div><span class="label">Total en letra:</span> {{totalEnLetra}}</div>
            {{/if}}
        </div>

        <!-- Descripción -->
        <div class="desc">{{{descripcion}}}</div>

        <!-- Firma arriba de la tabla -->
        <div class="sig">
            <div class="sig-inner">
                <div class="atte">Atentamente</div>
                <img src="assets/emp6-firma.png" alt="firma">
                <div class="name">Michelle Rodríguez Tlaxcalteca</div>
            </div>
        </div>

        <!-- TABLA -->
        <table>
            <thead>
                <tr>
                    <th class="center" style="width:16mm;">No.</th>
                    <th>Especificaciones</th>
                    <th class="center" style="width:24mm;">Cantidad</th>
                    <th class="right" style="width:30mm;">Precio unitario</th>
                    <th class="right" style="width:30mm;">Subtotal</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                <tr>
                    <td class="center">{{inc @index}}</td>
                    <td>{{this.nombre}}</td>
                    <td class="center">{{this.cantidad}}</td>
                    <td class="right">{{money this.unitario}}</td>
                    <td class="right">{{money this.parcial}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <!-- TOTALES -->
        <div class="grand">
            <table>
                <tr>
                    <td class="label">Subtotal</td>
                    <td class="value">{{money totales.subtotal}}</td>
                </tr>
                <tr>
                    <td class="label">IVA ({{ivaPct}}%)</td>
                    <td class="value">{{money totales.iva}}</td>
                </tr>
                <tr>
                    <td class="label">Total</td>
                    <td class="value">{{money totales.total}}</td>
                </tr>
            </table>
        </div>

        <!-- Términos -->
        <div class="terms-title">Términos:</div>
        <ul class="terms">
            <li>El importe total ya contiene el impuesto al valor agregado (IVA)</li>
            <li>La cotización se presenta en moneda nacional</li>
            <li>Las condiciones de pago serán: contado</li>
            <li>La vigencia de la cotización es de 20 días hábiles</li>
            <li>La entrega de los bienes se realizará a los 10 días naturales</li>
        </ul>
    </div>
</body>

</html>