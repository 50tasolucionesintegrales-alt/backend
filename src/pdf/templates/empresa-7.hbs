<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{titulo}}</title>
    <style>
        :root {
            --brand: #4c1d95;
            /* morado CHALOR */
            --ink: #111;
            --line: #d8d8e5;
            --thead: #4c1d95;
        }

        /* LETTER + dejo margen inferior para el pie del membrete */
        @page {
            size: Letter;
            margin: 16mm 18mm 40mm 18mm;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 12px;
            color: var(--ink);
        }

        /* ===== Membrete de fondo (sin recortes) ===== */
        .bg-page {
            position: fixed;
            inset: 0;
            z-index: -1;
            pointer-events: none;
        }

        .bg-page img {
            position: absolute;
            right: 0;
            bottom: 0;
            height: 100%;
            width: auto;
            display: block;
        }

        /* Contenido centrado */
        .content {
            max-width: 182mm;
            margin: 0 auto;
        }

        .header-space {
            height: 30mm;
        }

        /* Fecha */
        .fecha {
            color: #333;
            margin-bottom: 6mm;
        }

        /* Destinatario + PRESENTE */
        .dest {
            font-weight: 700;
            line-height: 1.45;
        }

        .presente {
            font-weight: 800;
            margin-top: 2px;
        }

        /* Descripción */
        .desc {
            margin-top: 10px;
            color: #222;
            line-height: 1.55;
        }

        /* ===== Tabla ===== */
        .table-wrap {
            margin-top: 10mm;
            position: relative;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead th {
            background: var(--thead);
            color: #fff;
            font-weight: 700;
            border: 1px solid var(--thead);
            padding: 8px;
            font-size: 12px;
            text-align: left;
        }

        tbody td {
            border: 1px solid var(--line);
            padding: 6px 8px;
            font-size: 12px;
            background: #fff;
            vertical-align: top;
        }

        .center {
            text-align: center;
        }

        .right {
            text-align: right;
        }

        /* Anchos similares al PDF */
        .col-idx {
            width: 12mm;
        }

        .col-marca {
            width: 24mm;
        }

        .col-cant {
            width: 22mm;
        }

        .col-uni {
            width: 20mm;
        }

        .col-unit {
            width: 28mm;
        }

        .col-subt {
            width: 30mm;
        }

        /* ===== Totales a la derecha ===== */
        .totals {
            display: flex;
            justify-content: flex-end;
            margin-top: 10px;
        }

        .totals table {
            width: 72mm;
            border-collapse: collapse;
        }

        .totals td {
            padding: 6px 8px;
            border-left: 1px solid var(--line);
            border-right: 1px solid var(--line);
        }

        .totals tr:first-child td {
            border-top: 1px solid var(--line);
        }

        .totals tr:last-child td {
            border-bottom: 1px solid var(--line);
        }

        .totals .label {
            font-weight: 700;
            background: #f7f5fb;
        }

        .totals .value {
            text-align: right;
        }

        /* ===== Términos + Firma (dos columnas) ===== */
        .tc-row {
            display: grid;
            grid-template-columns: 1fr 34%;
            /* <- antes 42% */
            gap: 14px;
            align-items: start;
            margin-top: 12px;
            page-break-inside: avoid;
        }

        .tc-box,
        .firma-box {
            border: 1px solid #bfb9d9;
            padding: 10px;
            background: #fff;
        }

        .tc-title {
            font-weight: 800;
            margin: 0 0 6px;
        }

        .tc-list {
            margin: 0;
            padding-left: 18px;
            color: #333;
        }

        .firma-box {
            text-align: center;
        }

        .firma-img {
            width: 70mm;
            /* <- tamaño controlado */
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto;
        }
    </style>
</head>

<body>

    <!-- Fondo -->
    <div class="bg-page"><img src="assets/emp7.png" alt="membrete"></div>

    <div class="content">
        <div class="header-space"></div>

        <!-- Fecha -->
        <div class="fecha">{{fecha}}</div>

        <!-- Destinatario -->
        <div class="dest">{{destinatario}}</div>
        <div class="presente">PRESENTE.</div>

        <!-- Descripción -->
        <div class="desc">{{{descripcion}}}</div>

        <!-- Tabla -->
        <div class="table-wrap">
            <table>
                <thead>
                    <tr>
                        <th class="center col-idx">#</th>
                        <th>Producto</th>
                        <th class="center col-marca">Marca</th>
                        <th class="center col-cant">Cantidad</th>
                        <th class="center col-uni">Unidad</th>
                        <th class="right  col-unit">Precio Unitario</th>
                        <th class="right  col-subt">Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each items}}
                    <tr>
                        <td class="center">{{inc @index}}</td>
                        <td>{{this.nombre}}</td>
                        <td class="center">{{this.marca}}</td>
                        <td class="center">{{this.cantidad}}</td>
                        <td class="center">{{this.unidad}}</td>
                        <td class="right">{{money this.unitario}}</td>
                        <td class="right">{{money this.parcial}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <!-- Totales -->
        <div class="totals">
            <table>
                <tr>
                    <td class="label">Subtotal</td>
                    <td class="value">{{money totales.subtotal}}</td>
                </tr>
                <tr>
                    <td class="label">I.V.A. ({{ivaPct}}%)</td>
                    <td class="value">{{money totales.iva}}</td>
                </tr>
                <tr>
                    <td class="label">Importe Total</td>
                    <td class="value">{{money totales.total}}</td>
                </tr>
            </table>
        </div>

        <!-- Términos & Firma (dos columnas) -->
        <div class="tc-row">
            <div class="tc-box">
                <div class="tc-title">Términos y Condiciones</div>
                <ul class="tc-list">
                    <li>La vigencia de la cotización es de 90 días naturales</li>
                    <li>Montos en moneda nacional</li>
                    <li>El pago debe ser realizado mediante transferencia</li>
                    <li>La entrega de los bienes se realizará a los 05 días hábiles</li>
                </ul>
            </div>
            <div class="firma-box">
                <img class="firma-img" src="assets/emp7-firma.png" alt="firma y sello">
            </div>
        </div>

    </div><!-- /content -->

</body>

</html>