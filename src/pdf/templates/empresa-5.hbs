<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{titulo}}</title>
    <style>
        :root {
            --gold: #b79b66;
            --ink: #111;
            --line: #d9d9d9;
            --thead: #f3f3f3;
        }

        @page {
            size: A4;
            margin: 16mm 18mm 42mm 18mm;
        }

        /* 👈 margen inferior amplio para footer */
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 12px;
            color: var(--ink);
        }

        /* Header */
        .header-img {
            width: 100%;
            height: auto;
            display: block;
        }

        /* Fecha */
        .fecha {
            text-align: right;
            color: #333;
            margin-top: 10px;
        }

        /* Destinatario */
        .dest {
            margin-top: 14px;
            font-weight: 700;
        }

        .presente {
            font-weight: 800;
            margin-top: 2px;
        }

        /* Descripción */
        .desc {
            margin-top: 12px;
            line-height: 1.55;
            color: #202020;
        }

        /* Tabla */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
        }

        thead th {
            background: var(--thead);
            color: #222;
            font-weight: 700;
            border: 1px solid var(--line);
            padding: 7px 9px;
            font-size: 12px;
            text-align: left;
        }

        tbody td {
            border: 1px solid var(--line);
            padding: 6px 8px;
            font-size: 12px;
            background: #fff;
        }

        .center {
            text-align: center;
        }

        .right {
            text-align: right;
        }

        /* Totales */
        .totals {
            display: flex;
            justify-content: flex-end;
            margin-top: 8px;
        }

        .totals table {
            width: 66mm;
            border-collapse: collapse;
        }

        .totals td {
            padding: 6px 8px;
            border-left: 1px solid var(--line);
            border-right: 1px solid var(--line);
        }

        .totals tr:first-child td {
            border-top: 1px solid var(--line);
        }

        .totals tr:last-child td {
            border-bottom: 1px solid var(--line);
        }

        .totals .label {
            font-weight: 700;
        }

        .totals .value {
            text-align: right;
        }

        /* Firma + Condiciones */
        .sig-conds {
            display: flex;
            gap: 24px;
            align-items: flex-start;
            margin-top: 18px;
        }

        .sig-col {
            width: 42%;
        }

        .conds-col {
            flex: 1;
        }

        .firma img {
            height: 96px;
            display: block;
        }

        .cond-title {
            font-weight: 800;
            margin: 0 0 6px;
        }

        .cond-list {
            margin: 0;
            padding-left: 18px;
            color: #333;
        }

        /* Footer contacto (imagen completa) */
        footer {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 10;
        }

        .footer-img {
            width: 100%;
            height: auto;
            display: block;
        }
    </style>
</head>

<body>

    <!-- HEADER -->
    <img class="header-img" src="assets/emp5.png" alt="header">

    <!-- FECHA -->
    <div class="fecha">Mineral de la Reforma, Hidalgo, a {{fecha}}</div>

    <!-- DESTINATARIO -->
    <div class="dest">{{destinatario}}</div>
    <div class="presente">PRESENTE.</div>

    <!-- DESCRIPCIÓN -->
    <div class="desc">{{{descripcion}}}</div>

    <!-- TABLA DINÁMICA -->
    <table>
        <thead>
            <tr>
                <th class="center" style="width:16mm;">No.</th>
                <th>Descripción</th>
                <th class="right" style="width:24mm;">Cantidad</th>
                <th class="center" style="width:24mm;">Medida</th>
                <th class="right" style="width:30mm;">Precio Unitario</th>
                <th class="right" style="width:30mm;">Subtotal</th>
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <td class="center">{{inc @index}}</td>
                <td>{{this.nombre}}</td>
                <td class="right">{{this.cantidad}}</td>
                <td class="center">{{this.unidad}}</td>
                <td class="right">{{money this.unitario}}</td>
                <td class="right">{{money this.parcial}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <!-- TOTALES -->
    <div class="totals">
        <table>
            <tr>
                <td class="label">Subtotal</td>
                <td class="value">{{money totales.subtotal}}</td>
            </tr>
            <tr>
                <td class="label">IVA ({{ivaPct}}%)</td>
                <td class="value">{{money totales.iva}}</td>
            </tr>
            <tr>
                <td class="label">Total</td>
                <td class="value">{{money totales.total}}</td>
            </tr>
        </table>
    </div>

    <!-- FIRMA + CONDICIONES -->
    <div class="sig-conds">
        <div class="sig-col">
            <div class="firma">
                <img src="assets/emp5-firma.png" alt="firma">
            </div>
        </div>
        <div class="conds-col">
            <div class="cond-title">Condiciones</div>
            <ul class="cond-list">
                <li>La entrega de los bienes se realizará a los 10 días hábiles después del pedido.</li>
                <li>La vigencia de la cotización es de 20 días naturales.</li>
                <li>El pago será de: Contado.</li>
                <li>La cotización se presenta en Moneda Nacional.</li>
            </ul>
        </div>
    </div>

    <!-- FOOTER CONTACTO -->
    <footer>
        <img class="footer-img" src="assets/emp5-footer.png" alt="footer">
    </footer>

</body>

</html>